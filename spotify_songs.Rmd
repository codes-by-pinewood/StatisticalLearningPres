---
title: "R Notebook"
output: html_notebook
---


```{r}
library(dplyr)
library(readr)
df <- read_csv("spotify-2023.csv", locale = locale(encoding = "UTF-8"))

print(df)

print(colnames(df))
print(class(df$track_name))
#install.packages("stringi")
library(stringi)

# Find rows where any column contains �
rows_to_remove <- which(apply(df, 1, function(row) any(stri_detect_fixed(row, "�"))))

# Remove those rows
df_cleaned <- df[-rows_to_remove, ]



```
